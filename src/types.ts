export interface Certificate {
  id: number;
  name: string;
  rut: string;
  career: string;
  graduation_year: number;
  whatsapp?: string;
  estado: string;
  created_at: string;
  qr_url?: string;
  view_url?: string;
  fecha_certificado: string;
  sede: string;
}